{
  "name": "@promptchain/portability",
  "version": "0.1.0",
  "description": "PromptChain Data Portability (Layer 17) - cross-protocol migration, promptpack verification, different-wallet restore",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@promptchain/storage": "^0.1.0",
    "@promptchain/schema": "^0.1.0",
    "@promptchain/backup": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^20.0.0"
  }
}
