{
  "name": "@promptchain/network",
  "version": "0.1.0",
  "description": "PromptChain P2P Networking Layer - libp2p-based gossip subnet, DHT, peer discovery, bandwidth accounting, offline-first sync",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
